---
layout: default
---

article#page.page
  h1= document.title
  if document.language
  h2=document.language
  .page-content
    nav.linklist
      if getCollection(document.relativeDirPath)
        each doc in getCollection(document.relativeDirPath).toJSON()
          .row
            div.col-xs-2
              a(href=doc.url)!= doc.title
            div
              | Last updated:&nbsp;
              span= doc.date.toDateString()
      else
        p
        | #{document.empty}
  .page-content!= content
